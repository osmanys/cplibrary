#include <bits/stdc++.h>
#define MAX 300000

using namespace std;
long t[MAX];
int op[MAX];

int main(){
	long n, a, b, c, d, start, len;
	cin >> n >> a >> b >> c >> d >> len;
	for(long i = 0; i < n; i++)
		scanf("%ld %d", &t[i], &op[i]);
	for(long i = 0; i < n; i++){
		
	}
	return 0;
}
