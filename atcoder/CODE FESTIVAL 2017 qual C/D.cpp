#include <bits/stdc++.h>
#define MAX 200000

using namespace std;
vector<char> p[MAX];
long p[MAX];
long 

int main(){
	string s;
	getline(cin, s);
	int c = 0;
	for(string::iterator it = s.begin(); it != s.end(); it++){
		
	}
	cout << (!c ? 1 : c);
	return 0;
}
